extend base.pug

block content
    if errorMessage
        span=errorMessage
    h5 아이디가 없으세요?; 
    a(href="join") 회원가입하기 &rarr;
    form(method="POST")
        input(placeholder="학번", required, type="text", name="schoolID")
        input(placeholder="비밀번호", required, type="password", name="password")
        input(value = "로그인", type="submit",onClick="window.location.reload()")
        br
        a(href="/user/github/start") Continue with Github &rarr;
    a(href="locker") 사물함 신청 페이지로 가기 &rarr;

