doctype html
html(lang="ko")
    head
        title CNU Locker | #{pagetitle}
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            h1 ⌛️CNU Electronic Engineering Locker Application Program⏳
            h3 This is #{pagetitle} page
            nav
                ul
                    li
                        a(href="/locker") Locker
                    
                    if loggedOn
                        li
                            a(href="/logout") Log out
                        li
                            a(href="/my-profile") #{loggedOnUser.name}의 프로필
                    else
                        li
                            a(href="/") Login
                    li
                        a(href="/user/comment") comment
                    li
                        a(href="/user/edit") edit user
        main
            block content
    include partials/footer.pug